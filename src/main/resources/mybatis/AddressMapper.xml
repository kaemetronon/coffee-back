<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.coffee.nostresso.model.mapper.AddressMapper">

    <select id="getByShopId" resultType="ru.coffee.nostresso.model.entity.Address">

    </select>

    <update id="updateAddress">
        insert into address (address_text, x, y, shop_id)
        values (#{addressText},
                #{x},
                #{y},
                #{shopId, javaType=java.util.UUID,jdbcType=OTHER,typeHandler=UuidTypeHandler});
        update address
        set is_deleted= true,
            date_updated = now()
        where id = #{id, javaType=java.util.UUID,jdbcType=OTHER,typeHandler=UuidTypeHandler}
    </update>

    <delete id="deleteByShopId">
        update address
        set is_deleted= true,
            date_updated = now()
        where id = #{id, javaType=java.util.UUID,jdbcType=OTHER,typeHandler=UuidTypeHandler}
    </delete>
</mapper>
